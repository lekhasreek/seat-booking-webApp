import React, { useState } from 'react';
import SidebarLink from './SidebarLink';

const Sidebar = () => {
    const [workspacesOpen, setWorkspacesOpen] = useState(false);
    return (
// Sidebar component removed
// SidebarLink component removed
    );
};

export default Sidebar;
